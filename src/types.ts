export interface Country {
    name: string;
    code: string;
}

export interface CountryInfo {
    name: string;
    code: string;
    borders: string[];
    capital: string;
    flag: string;
    region: string;
}